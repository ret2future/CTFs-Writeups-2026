apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-service"
  namespace: "{{ .Release.Namespace }}"
spec:
  type: LoadBalancer
  loadBalancerIP: 34.133.200.18
  selector:
    app: "{{ .Release.Name }}"
  ports:
    - protocol: TCP
      port: 160
      targetPort: 8000
